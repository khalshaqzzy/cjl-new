// Font configuration — Google Fonts only, no local files
// This file exists to anchor the font build graph for Turbopack cache invalidation.
// Fonts are loaded in app/layout.tsx via next/font/google.
export const FONT_DISPLAY = "Plus Jakarta Sans"
export const FONT_BODY = "DM Sans"
